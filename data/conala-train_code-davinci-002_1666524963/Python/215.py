def chunks(l, n):
    """Yield successive n-sized chunks from l."""
    for i in range(0, len(l), n):
        yield l[i:i + n]

def chunks_str(s, n):
    """Yield successive n-sized chunks from s."""
    for i in range(0, len(s), n):
        yield s[i:i + n]

def chunks_str_list(s, n):
    """Yield successive n-sized chunks from s."""
    for i in range(0, len(s), n):
        yield s[i:i + n]

def chunks_str_list_2(s, n):
    """Yield successive n-sized chunks from s."""
    for i in range(0, len(s), n):
        yield s[i:i + n]

def chunks_str_list_3(s, n):
    """Yield successive n-sized chunks from s."""
    for i in range(0, len(s), n):
        yield s[i:i + n]

def chunks_str_list_4(s, n):
    """Yield successive n-sized chunks from s."""
    for i in range(0, len(s), n):
        yield s[i:i + n]

def chunks_str_list_5(s, n):
    """Yield successive n-sized chunks from s."""
    for i in range(0, len(s), n):
        yield s[i:i + n]

def chunks_str_list_6(s, n):
    """Yield successive n-sized chunks from s."""
    for i in range(0, len(s), n):
        yield s[i:i + n]

def chunks_str_list_7(s, n):
    """Yield successive n-sized chunks from s."""
    for i in range(0, len(s), n):
        yield s[i:i + n]

def chunks_str_list_8(s, n):